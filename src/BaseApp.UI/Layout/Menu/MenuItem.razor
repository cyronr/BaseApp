@inherits NavLink
@inject NavigationManager NavigationManager

<li>
    <NavLink href="@To" class="@CssClass">@Text</NavLink>
@*     <a href="@To" class="@CssClass">
        @Text
    </a> *@
</li>

@code {
    [Parameter]
    public string To { get; set; }

    [Parameter]
    public string Text { get; set; }

    private string CssClass => 
        NavigationManager.Uri.Contains(To) ? 
            "block py-2 px-3 text-light-gray-100 bg-blue-700 rounded md:bg-transparent md:text-light-gray-100 md:p-0 md:text-l"
            :
            "block py-2 px-3 text-light-gray-500 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-light-gray-100 md:p-0 md:text-l";
}
